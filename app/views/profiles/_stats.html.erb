<!-- Stats Grid -->
<%= render Profiles::StatsGridComponent.new(profile: profile) %>

<!-- Organizations (if any) -->
<div class="p-8 border-b border-slate-700 <%= 'hidden' unless profile.organizations.present? && profile.organizations.any? %>"
     data-profile-organizations-container>
  <h2 class="text-xl font-bold text-white mb-4 flex items-center gap-2">
    <%= render Ui::IconComponent.new(name: :office_building, size: :lg, css_class: "text-indigo-400") %>
    Organizações
  </h2>
  <div class="flex flex-wrap gap-3" data-profile-organizations-list>
    <% if profile.organizations.present? && profile.organizations.any? %>
      <% profile.organizations.each do |org| %>
        <span class="inline-flex items-center gap-2 px-4 py-2 bg-slate-900/50 border border-slate-600 rounded-lg text-gray-300 hover:bg-slate-900 hover:border-indigo-500/50 transition-all">
          <%= org %>
        </span>
      <% end %>
    <% end %>
  </div>
</div>

<!-- Additional Info -->
<div class="p-8 bg-slate-900/30">
  <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 text-sm">
    <%= render Profiles::InfoCardComponent.new(
      icon: :clock,
      label: "Último scan",
      value: profile.last_scanned_at.present? ? "#{time_ago_in_words(profile.last_scanned_at)} atrás" : "",
      show: profile.last_scanned_at.present?
    ) %>

    <%= render Profiles::InfoCardComponent.new(
      icon: :calendar,
      label: "Adicionado em",
      value: l(profile.created_at, format: :short),
      show: true
    ) %>
  </div>

  <div class="mt-4 p-4 bg-red-500/10 border border-red-500/30 rounded-lg <%= 'hidden' unless profile.last_error.present? %>"
       data-profile-error-container>
    <p class="text-sm text-red-400">
      <span class="font-semibold">Erro:</span>
      <span data-profile-error><%= profile.last_error %></span>
    </p>
  </div>
</div>
